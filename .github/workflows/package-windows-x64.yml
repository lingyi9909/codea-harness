name: package-windows-x64

on:
  workflow_dispatch:
  pull_request:
    branches: [develop]
    paths:
      - '.code-harness/**'
      - 'README.md'
      - 'CHANGELOG.md'
      - 'THIRD_PARTY_NOTICES.md'
      - '.github/workflows/package-windows-x64.yml'
  push:
    branches: [develop]
    paths:
      - '.code-harness/**'
      - 'README.md'
      - 'CHANGELOG.md'
      - 'THIRD_PARTY_NOTICES.md'
      - '.github/workflows/package-windows-x64.yml'

permissions:
  contents: read

jobs:
  package:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v7
        with:
          go-version: '1.23.x'

      - name: 1.5 exact controlled runtime release gate
        working-directory: .code-harness/tools-runtime
        shell: pwsh
        run: |
          go test -count=1 ./internal/chain ./internal/reviewscope ./internal/coverage ./internal/report
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          go test -count=1 ./internal/apply ./internal/schema ./internal/upgrade
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          go test -count=1 ./...
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          go vet ./...
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Build Windows x64 runtime
        working-directory: .code-harness/tools-runtime
        shell: pwsh
        run: |
          $env:GOOS = 'windows'
          $env:GOARCH = 'amd64'
          go build -trimpath -ldflags "-s -w" -o ../bin/codea-harness-tools.exe ./cmd/codea-harness-tools
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Vendor pinned ast-grep Windows x64
        shell: pwsh
        run: |
          $version = '0.42.1'
          $expected = 'fe34f631bb24c08ad146f92ca2a92971a53d179461b509fd8d32dc863bff9f83'
          $zip = Join-Path $env:RUNNER_TEMP 'ast-grep.zip'
          $dir = Join-Path $env:RUNNER_TEMP 'ast-grep'
          $url = "https://github.com/ast-grep/ast-grep/releases/download/$version/app-x86_64-pc-windows-msvc.zip"
          Invoke-WebRequest -Uri $url -OutFile $zip
          $actual = (Get-FileHash -Algorithm SHA256 $zip).Hash.ToLowerInvariant()
          if ($actual -ne $expected) { throw "ast-grep checksum mismatch: $actual" }
          Expand-Archive -Path $zip -DestinationPath $dir -Force
          Copy-Item (Join-Path $dir 'ast-grep.exe') '.code-harness/bin/ast-grep.exe'
          & '.code-harness/bin/ast-grep.exe' --version
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Lightweight Code Navigation real ast-grep smoke
        shell: pwsh
        run: |
          New-Item -ItemType Directory -Force 'smoke/src/main/java' | Out-Null
          @'
          public interface OrderService { void approve(); }
          '@ | Set-Content 'smoke/src/main/java/OrderService.java'
          @'
          public interface AuditService { void approve(); }
          '@ | Set-Content 'smoke/src/main/java/AuditService.java'
          @'
          @RestController
          public class OrderController {
              private final OrderService orderService;
              private final AuditService auditService;
              public OrderController(OrderService orderService, AuditService auditService) {
                  this.orderService = orderService;
                  this.auditService = auditService;
              }
              @PostMapping(
                  value = "/approve"
              )
              public Result<Void> approve(
                  @RequestParam("id") Long id,
                  @RequestParam(required = false) String reason) {
                  orderService.approve();
                  auditService.approve();
                  return null;
              }
          }
          '@ | Set-Content 'smoke/src/main/java/OrderController.java'

          $info = & '.code-harness/bin/codea-harness-tools.exe' nav get-symbol-info --symbol OrderController.approve --scope smoke/src/main/java
          if ($LASTEXITCODE -ne 0) { throw "get-symbol-info failed: $($info -join "`n")" }
          $infoText = $info -join "`n"
          if ($infoText -notmatch 'OrderController.approve') { throw "symbol info missing: $infoText" }
          if ($infoText -notmatch 'RequestParam') { throw "Spring parameter annotations missing: $infoText" }
          if ($infoText -notmatch '/approve') { throw "multiline PostMapping value missing: $infoText" }

          $ann = & '.code-harness/bin/codea-harness-tools.exe' nav find-by-annotation --annotation PostMapping --scope smoke/src/main/java
          if ($LASTEXITCODE -ne 0) { throw "find-by-annotation failed: $($ann -join "`n")" }
          if (($ann -join "`n") -notmatch 'OrderController.approve') { throw 'PostMapping method not found' }

          $callers = & '.code-harness/bin/codea-harness-tools.exe' nav find-callers --symbol OrderService.approve --scope smoke/src/main/java
          if ($LASTEXITCODE -ne 0) { throw "find-callers failed: $($callers -join "`n")" }
          $callerText = $callers -join "`n"
          if ($callerText -notmatch 'CONFIRMED') { throw "typed OrderService caller not confirmed: $callerText" }
          if ($callerText -match 'AuditService.*CONFIRMED') { throw "AuditService call incorrectly confirmed: $callerText" }
          Remove-Item -Recurse -Force smoke

      - name: Stage formal 1.5.0 install and upgrade releases
        shell: pwsh
        run: |
          $version = (Get-Content '.code-harness/VERSION' -Raw).Trim()
          if ($version -ne '1.5.0') { throw "unexpected release version: $version" }

          $runtimeHash = (Get-FileHash -Algorithm SHA256 '.code-harness/bin/codea-harness-tools.exe').Hash.ToLowerInvariant()
          $astHash = (Get-FileHash -Algorithm SHA256 '.code-harness/bin/ast-grep.exe').Hash.ToLowerInvariant()

          $installStage = Join-Path $env:RUNNER_TEMP 'codea-release-install'
          $upgradeStage = Join-Path $env:RUNNER_TEMP 'codea-release-upgrade'
          Remove-Item -Recurse -Force $installStage,$upgradeStage -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Force $installStage,$upgradeStage | Out-Null

          Copy-Item -Recurse '.code-harness' (Join-Path $installStage '.code-harness')
          Copy-Item -Recurse '.code-harness' (Join-Path $upgradeStage '.code-harness-upgrade')

          foreach ($releaseRoot in @(
            (Join-Path $installStage '.code-harness'),
            (Join-Path $upgradeStage '.code-harness-upgrade')
          )) {
            foreach ($state in @('harness.yaml','project.md','database.yaml','runs','chains')) {
              Remove-Item -Recurse -Force (Join-Path $releaseRoot $state) -ErrorAction SilentlyContinue
            }
          }

          $manifest = [ordered]@{
            version = $version
            platform = 'windows'
            arch = 'x64'
            runtime = 'codea-harness-tools.exe'
            runtimeSha256 = $runtimeHash
            astGrepVersion = '0.42.1'
            astGrepSha256 = $astHash
          } | ConvertTo-Json

          [System.IO.File]::WriteAllText((Join-Path $installStage '.code-harness/RELEASE-MANIFEST.json'), $manifest, [System.Text.UTF8Encoding]::new($false))
          [System.IO.File]::WriteAllText((Join-Path $upgradeStage '.code-harness-upgrade/RELEASE-MANIFEST.json'), $manifest, [System.Text.UTF8Encoding]::new($false))

          $commonRequired = @(
            'VERSION','RELEASE-MANIFEST.json','AGENTS.md','bootstrap.md','upgrade.md',
            'harness.template.yaml','project.template.md','bin/codea-harness-tools.exe','bin/ast-grep.exe',
            'agents/orchestrator.md','agents/reviewer.md','agents/api-doc-agent.md',
            'skills/analyze-change/SKILL.md','skills/review-code/SKILL.md',
            'skills/discover-api/SKILL.md','skills/generate-api-doc/SKILL.md',
            'skills/discover-chain/SKILL.md','skills/validate-chain/SKILL.md',
            'contracts/change-analysis.schema.json','contracts/review-output.schema.json','contracts/review-scope.schema.json',
            'contracts/apply-request.schema.json','contracts/apply-result.schema.json',
            'contracts/chain.schema.json','contracts/chain-validation-result.schema.json',
            'templates/chain.template.yaml',
            'tools-runtime/internal/chain/model.go','tools-runtime/internal/chain/validate.go',
            'tools-runtime/internal/reviewscope/chain_context.go',
            'tools-runtime/cmd/codea-harness-tools/chain_command.go',
            'tools/README.md'
          )

          foreach ($pair in @(
            @{ Root = (Join-Path $installStage '.code-harness'); Name = 'install' },
            @{ Root = (Join-Path $upgradeStage '.code-harness-upgrade'); Name = 'upgrade' }
          )) {
            foreach ($rel in $commonRequired) {
              if (!(Test-Path (Join-Path $pair.Root $rel))) { throw "$($pair.Name) release missing: $rel" }
            }
            foreach ($dir in @('agents','skills','contracts','templates','tools','tools-runtime')) {
              if (@(Get-ChildItem (Join-Path $pair.Root $dir) -File -Recurse).Count -eq 0) { throw "$($pair.Name) release has empty $dir tree" }
            }
            foreach ($state in @('harness.yaml','project.md','database.yaml','runs','chains')) {
              if (Test-Path (Join-Path $pair.Root $state)) { throw "$($pair.Name) release leaked Project State: $state" }
            }
            $m = Get-Content (Join-Path $pair.Root 'RELEASE-MANIFEST.json') -Raw | ConvertFrom-Json
            if ($m.version -ne $version -or $m.platform -ne 'windows' -or $m.arch -ne 'x64') { throw "$($pair.Name) manifest identity invalid" }
            if ($m.runtime -ne 'codea-harness-tools.exe' -or $m.astGrepVersion -ne '0.42.1') { throw "$($pair.Name) manifest tool identity invalid" }
            if ($m.runtimeSha256 -ne $runtimeHash -or $m.astGrepSha256 -ne $astHash) { throw "$($pair.Name) manifest hashes invalid" }
          }

          $bootstrap = Get-Content (Join-Path $upgradeStage '.code-harness-upgrade/upgrade.md') -Raw
          foreach ($needle in @(
            '.code-harness-upgrade/RELEASE-MANIFEST.json',
            '.code-harness-upgrade/bin/codea-harness-tools.exe',
            '.code-harness-upgrade/bin/ast-grep.exe',
            'contracts/harness-config.schema.json',
            'contracts/chain.schema.json',
            'templates/chain.template.yaml',
            'MANUAL_ACTION_REQUIRED',
            '不调用任何 Runtime',
            'GitHub Source Code',
            '.code-harness-upgrade/bin/codea-harness-tools.exe upgrade',
            'chains/**'
          )) {
            if ($bootstrap -notmatch [regex]::Escape($needle)) { throw "upgrade bootstrap contract missing: $needle" }
          }

      - name: Build 1.5.0 install and upgrade ZIPs
        shell: pwsh
        run: |
          $installStage = Join-Path $env:RUNNER_TEMP 'codea-release-install'
          $upgradeStage = Join-Path $env:RUNNER_TEMP 'codea-release-upgrade'
          Compress-Archive -Path (Join-Path $installStage '.code-harness') -DestinationPath 'codea-harness-1.5.0-windows-x64-install.zip' -Force
          Compress-Archive -Path (Join-Path $upgradeStage '.code-harness-upgrade') -DestinationPath 'codea-harness-1.5.0-windows-x64-upgrade.zip' -Force

      - name: Validate ZIP layouts and real accepted 1.4.0 to 1.5.0 Windows live upgrade
        shell: pwsh
        run: |
          $installExtract = Join-Path $env:RUNNER_TEMP 'codea-install-extract'
          $upgradeExtract = Join-Path $env:RUNNER_TEMP 'codea-upgrade-extract'
          Remove-Item -Recurse -Force $installExtract,$upgradeExtract -ErrorAction SilentlyContinue
          Expand-Archive 'codea-harness-1.5.0-windows-x64-install.zip' $installExtract -Force
          Expand-Archive 'codea-harness-1.5.0-windows-x64-upgrade.zip' $upgradeExtract -Force

          if (!(Test-Path (Join-Path $installExtract '.code-harness'))) { throw 'install.zip must extract directly to .code-harness/' }
          if (!(Test-Path (Join-Path $upgradeExtract '.code-harness-upgrade'))) { throw 'upgrade.zip must extract directly to .code-harness-upgrade/' }
          if (Test-Path (Join-Path $installExtract '.code-harness-upgrade')) { throw 'install.zip contains wrong top-level harness directory' }
          if (Test-Path (Join-Path $upgradeExtract '.code-harness')) { throw 'upgrade.zip contains wrong top-level harness directory' }

          $required15 = @(
            'VERSION','RELEASE-MANIFEST.json','AGENTS.md','bootstrap.md','upgrade.md',
            'harness.template.yaml','project.template.md','bin/codea-harness-tools.exe','bin/ast-grep.exe',
            'contracts/chain.schema.json','contracts/chain-validation-result.schema.json',
            'templates/chain.template.yaml','skills/discover-chain/SKILL.md','skills/validate-chain/SKILL.md',
            'tools-runtime/internal/chain/model.go','tools-runtime/internal/reviewscope/chain_context.go',
            'tools-runtime/cmd/codea-harness-tools/chain_command.go'
          )
          foreach ($releaseRoot in @((Join-Path $installExtract '.code-harness'), (Join-Path $upgradeExtract '.code-harness-upgrade'))) {
            foreach ($rel in $required15) {
              if (!(Test-Path (Join-Path $releaseRoot $rel))) { throw "extracted 1.5 release missing: $rel" }
            }
            foreach ($state in @('harness.yaml','project.md','database.yaml','runs','chains')) {
              if (Test-Path (Join-Path $releaseRoot $state)) { throw "release ZIP leaked Project State: $state" }
            }
          }

          $baseline = 'bedf2cde3784a6ee15d408271a023a95570c46b8'
          $root = Join-Path $env:RUNNER_TEMP 'codea-real-upgrade-140-to-150'
          Remove-Item -Recurse -Force $root -ErrorAction SilentlyContinue
          New-Item -ItemType Directory -Force $root | Out-Null

          $baselineZip = Join-Path $env:RUNNER_TEMP 'codea-baseline-140.zip'
          Remove-Item $baselineZip -Force -ErrorAction SilentlyContinue
          git cat-file -e "$baseline^{commit}"
          if ($LASTEXITCODE -ne 0) { throw "accepted 1.4 baseline commit is unavailable: $baseline" }
          git archive --format=zip --output $baselineZip $baseline .code-harness
          if ($LASTEXITCODE -ne 0) { throw 'failed to archive accepted 1.4 baseline' }
          Expand-Archive $baselineZip $root -Force

          $target = Join-Path $root '.code-harness'
          if ((Get-Content (Join-Path $target 'VERSION') -Raw).Trim() -ne '1.4.0') { throw 'accepted baseline archive is not VERSION=1.4.0' }

          New-Item -ItemType Directory -Force (Join-Path $target 'bin') | Out-Null
          [System.IO.File]::WriteAllBytes((Join-Path $target 'bin/codea-harness-tools.exe'), [byte[]](79,76,68,45,49,46,52,46,48))
          New-Item -ItemType Directory -Force (Join-Path $target 'skills/stale-140') | Out-Null
          [System.IO.File]::WriteAllText((Join-Path $target 'skills/stale-140/SKILL.md'), 'stale-140', [System.Text.UTF8Encoding]::new($false))

          $harnessYaml = @'
          version: 2
          project:
            type: maven
            root: .
            module: ""
          review:
            baseRef: origin/release-user-custom
            includeWorkingTree: false
          integrationTest:
            executable: mvn
            args: [test]
            reportDir: target/surefire-reports
            timeoutSeconds: 777
          service:
            executable: mvn
            args: [spring-boot:run]
            startupTimeoutSeconds: 120
            readiness:
              type: log
              pattern: Started
            logFile: null
          stopService:
            mode: processTree
          initialization:
            status: READY
            unresolved: []
          scope:
            sourceIncludes:
              - modules/order/src/main/java/**
            testIncludes:
              - modules/order/src/test/java/**
            mapperIncludes:
              - modules/order/src/main/resources/**/*Mapper.xml
            configIncludes:
              - modules/order/src/main/resources/**/*.yml
          write:
            allowedTestPaths: [src/test/**]
            allowedProductionPaths: [src/main/**]
            deniedPaths: []
          runs:
            directory: .code-harness/runs
          '@
          [System.IO.File]::WriteAllText((Join-Path $target 'harness.yaml'), $harnessYaml, [System.Text.UTF8Encoding]::new($false))
          [System.IO.File]::WriteAllText((Join-Path $target 'project.md'), "project-state-sentinel-140`r`n", [System.Text.UTF8Encoding]::new($false))
          [System.IO.File]::WriteAllText((Join-Path $target 'database.yaml'), "version: 1`r`nenvironment: TEST`r`npassword: sentinel-secret`r`n", [System.Text.UTF8Encoding]::new($false))
          New-Item -ItemType Directory -Force (Join-Path $target 'runs') | Out-Null
          [System.IO.File]::WriteAllBytes((Join-Path $target 'runs/keep.bin'), [byte[]](1,2,3,4,5,6))
          New-Item -ItemType Directory -Force (Join-Path $target 'chains') | Out-Null
          $sentinelChain = "# user-owned chain`r`nversion: 1`r`nid: order-approve`r`nname: 订单审批`r`nstatus: ACCEPTED`r`nentryPoints: []`r`nnodes: []`r`nresources: []`r`nboundaries: []`r`nnotes: preserve exactly`r`n"
          [System.IO.File]::WriteAllText((Join-Path $target 'chains/order-approve.yaml'), $sentinelChain, [System.Text.UTF8Encoding]::new($false))

          $stateHashes = @{
            harness = (Get-FileHash -Algorithm SHA256 (Join-Path $target 'harness.yaml')).Hash
            project = (Get-FileHash -Algorithm SHA256 (Join-Path $target 'project.md')).Hash
            database = (Get-FileHash -Algorithm SHA256 (Join-Path $target 'database.yaml')).Hash
            runs = (Get-FileHash -Algorithm SHA256 (Join-Path $target 'runs/keep.bin')).Hash
            chain = (Get-FileHash -Algorithm SHA256 (Join-Path $target 'chains/order-approve.yaml')).Hash
          }

          Copy-Item -Recurse (Join-Path $upgradeExtract '.code-harness-upgrade') (Join-Path $root '.code-harness-upgrade')
          $newRuntime = Join-Path $root '.code-harness-upgrade/bin/codea-harness-tools.exe'
          $expectedRuntimeHash = (Get-FileHash -Algorithm SHA256 $newRuntime).Hash

          Push-Location $root
          try {
            $out = & '.\.code-harness-upgrade\bin\codea-harness-tools.exe' upgrade 2>&1
            if ($LASTEXITCODE -ne 0) { throw "real accepted 1.4.0 -> 1.5.0 live upgrade failed: $($out -join "`n")" }
            $text = $out -join "`n"
            if ($text -notmatch '"status": "UPGRADED"') { throw "unexpected upgrade output: $text" }
            if ($text -notmatch 'skills/stale-140/SKILL.md') { throw "removedFiles missing stale 1.4 framework path: $text" }
            if ($text -match 'chains/order-approve.yaml') { throw "business chain leaked into managed update/remove evidence: $text" }

            if ((Get-Content '.code-harness/VERSION' -Raw).Trim() -ne '1.5.0') { throw 'target VERSION is not 1.5.0 after live upgrade' }
            if ((Get-FileHash -Algorithm SHA256 '.code-harness/harness.yaml').Hash -ne $stateHashes.harness) { throw 'harness.yaml changed during 1.4.0 -> 1.5.0 upgrade' }
            if ((Get-FileHash -Algorithm SHA256 '.code-harness/project.md').Hash -ne $stateHashes.project) { throw 'project.md changed during 1.4.0 -> 1.5.0 upgrade' }
            if ((Get-FileHash -Algorithm SHA256 '.code-harness/database.yaml').Hash -ne $stateHashes.database) { throw 'database.yaml changed during 1.4.0 -> 1.5.0 upgrade' }
            if ((Get-FileHash -Algorithm SHA256 '.code-harness/runs/keep.bin').Hash -ne $stateHashes.runs) { throw 'runs/** changed during 1.4.0 -> 1.5.0 upgrade' }
            if ((Get-FileHash -Algorithm SHA256 '.code-harness/chains/order-approve.yaml').Hash -ne $stateHashes.chain) { throw 'chains/** changed during 1.4.0 -> 1.5.0 upgrade' }

            foreach ($path in @(
              '.code-harness/contracts/chain.schema.json',
              '.code-harness/contracts/chain-validation-result.schema.json',
              '.code-harness/templates/chain.template.yaml',
              '.code-harness/skills/discover-chain/SKILL.md',
              '.code-harness/skills/validate-chain/SKILL.md',
              '.code-harness/tools-runtime/internal/chain/model.go',
              '.code-harness/tools-runtime/internal/reviewscope/chain_context.go'
            )) {
              if (!(Test-Path $path)) { throw "1.5 Chain Framework path missing after live upgrade: $path" }
            }
            if (Test-Path '.code-harness/skills/stale-140/SKILL.md') { throw 'stale 1.4 Framework survived live upgrade' }
            if (Test-Path '.code-harness-upgrade') { throw 'upgrade source was not cleaned' }
            if (Get-ChildItem -Force -Name '.code-harness-stage-*' -ErrorAction SilentlyContinue) { throw 'stage directory leaked' }
            if (Get-ChildItem -Force -Name '.code-harness-backup-*' -ErrorAction SilentlyContinue) { throw 'backup directory leaked' }
            if ((Get-FileHash -Algorithm SHA256 '.code-harness/bin/codea-harness-tools.exe').Hash -ne $expectedRuntimeHash) { throw 'installed runtime does not match accepted 1.5 release runtime' }

            # Probe installed chain validate capability through its deterministic argument gate.
            $probeOut = Join-Path $env:RUNNER_TEMP 'codea-chain-validate-probe.out'
            $probeErr = Join-Path $env:RUNNER_TEMP 'codea-chain-validate-probe.err'
            Remove-Item $probeOut,$probeErr -Force -ErrorAction SilentlyContinue
            $probeExe = (Resolve-Path '.\.code-harness\bin\codea-harness-tools.exe').Path
            $probe = Start-Process -FilePath $probeExe -ArgumentList @('chain','validate') -Wait -PassThru -RedirectStandardOutput $probeOut -RedirectStandardError $probeErr
            $probeText = @(
              Get-Content $probeOut -ErrorAction SilentlyContinue
              Get-Content $probeErr -ErrorAction SilentlyContinue
            ) -join "`n"
            if ($probe.ExitCode -eq 0 -or $probeText -notmatch 'chain validate requires --id and --change-analysis') {
              throw "installed 1.5 runtime does not expose chain validate capability: $probeText"
            }
            $global:LASTEXITCODE = 0
          } finally {
            Pop-Location
          }

      - name: Upload 1.5.0 install package
        uses: actions/upload-artifact@v4
        with:
          name: codea-harness-1.5.0-windows-x64-install
          path: codea-harness-1.5.0-windows-x64-install.zip
          if-no-files-found: error

      - name: Upload 1.5.0 upgrade package
        uses: actions/upload-artifact@v4
        with:
          name: codea-harness-1.5.0-windows-x64-upgrade
          path: codea-harness-1.5.0-windows-x64-upgrade.zip
          if-no-files-found: error
