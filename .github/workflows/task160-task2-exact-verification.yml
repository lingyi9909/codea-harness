name: task160-task2-exact-verification

on:
  pull_request:
    branches: [develop]
    paths:
      - '.code-harness/**'
      - '.github/workflows/task160-task2-exact-verification.yml'

permissions:
  contents: read

jobs:
  exact-task2-gate:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - uses: actions/setup-go@v7
        with:
          go-version: '1.26.5'

      - name: Task 2 exact package gate
        working-directory: .code-harness/tools-runtime
        shell: pwsh
        run: go test -count=1 ./internal/reviewrules ./cmd/codea-harness-tools

      - name: Task 2 exact regression package gate
        working-directory: .code-harness/tools-runtime
        shell: pwsh
        run: go test -count=1 ./internal/reviewrules ./internal/reviewunit ./cmd/codea-harness-tools

      - name: Task 2 exact full test gate
        working-directory: .code-harness/tools-runtime
        shell: pwsh
        run: go test -count=1 ./...

      - name: Task 2 exact vet gate
        working-directory: .code-harness/tools-runtime
        shell: pwsh
        run: go vet ./...
