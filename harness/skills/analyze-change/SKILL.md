# Analyze Git Change

Use `git_diff` to list changed files and hunks. Read changed source and directly related callers/callees with `read_code`. Produce affected Controllers, internal call chains, external dependencies, and risk areas. Do not scan unrelated modules or modify files.
