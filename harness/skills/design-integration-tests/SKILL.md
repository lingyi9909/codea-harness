# Design Integration Tests

Map affected Controllers to real Service and Repository chains. Produce `test-plan.schema.json` with request, preconditions, external mocks, expected HTTP result, response, state transition, database effects, and error cases. Set `approved` false until a human approves.
