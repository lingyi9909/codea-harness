# Analyze Failure

Correlate Maven output, Surefire reports, stack traces, and run-window logs. Emit `diagnosis.schema.json` using exactly one supported classification. Distinguish test code, context, environment/data, service startup, and production defects. Do not modify code.
