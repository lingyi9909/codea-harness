# Run Integration Tests

Use `run_maven_test` with the configured executable and argument list; never construct arbitrary shell. Read stdout/stderr and Surefire results with `read_test_report`. Limit execution to selected test classes and configured timeout.
