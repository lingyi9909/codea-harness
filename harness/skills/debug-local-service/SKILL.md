# Debug Local Service

Use `start_service` with fixed configuration, record the returned PID and run window, capture stdout/stderr, verify readiness, then read relevant application logs. Use `stop_service` only for the PID created by the same run. Requests remain manual in V1.
