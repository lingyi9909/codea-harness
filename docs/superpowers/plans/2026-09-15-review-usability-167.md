# 1.6.7 B-end Review usability repair

Approved scope: make a typical Controller review fast and complete within the existing architecture. Keep main-session semantic review, Runtime-owned facts/certification/report, and mandatory next-user selection whenever more than one actual chain is available. No persistent index, new graph subsystem, complex resume, or unrelated 1.7 work.

## Global constraints

- Preserve exact symbol/path/workspace authority, ambiguity rejection, FULL coverage and human selection gates.
- Preserve installed offline Windows layout and managed-file ownership. Publish a new 1.6.7 upgrade instead of replacing 1.6.6.
- Do not silently treat process failure, timeout, malformed output or incomplete scan as empty success.
- No generic Reviewer delegation in the product's normal review path.
- Full Go and vet plus real Windows AST/Host/report/upgrade evidence are required. Model-fixture transport tests must not be described as proof of private-model reasoning or instruction adherence.

## Task 1: Batch navigation and finding symbol verification

Replace per-pattern process loops in annotation and symbol-info queries with bounded AST batches while preserving matching semantics and exact scope. Reuse in-memory declaration results for symbols in the same file during finding certification; file inputs must not change unnoticed. Preserve class/method/field, annotations, overload ambiguity, nested classes and Windows paths. Add meaningful real ast-grep parity, process-count, malformed/failure/cancellation and scale regressions. Keep public CLI compatible. Coordinate any new nav API with the controller; own internal/nav and internal/finding verification only.

## Task 2: Submission preflight and reliable class-review entry

Reject invalid chainRefs length/order/symbol and malformed proposal shape before writing proposal/receipt or advancing progress. Use shared Runtime validation where possible; never invent evidence. Establish a concise installed main-agent entry for targeted Controller review that creates runId before semantic work and finishes with Runtime report. Scope code reading to actual target dependencies without weakening certified coverage. Stop on actual failures with useful diagnostic evidence; no automatic restart loops or complex recovery. Preserve user-owned Host configuration. Test valid and malformed submission, no-write rejection, and entry-to-report behavior.

## Task 3: Windows evidence and 1.6.7 delivery

Verify a representative Controller-Service-Mapper/SQL project with real pinned ast-grep, primary Host transport, same-run creation, analysis, selection, findings certification and report. Include two-chain human selection refusal before reply. Record Runtime timing and AST process counts; separately label deterministic model-fixture evidence. Prepare exact-head install/upgrade, real published 1.6.6 upgrade preservation, full Windows go test -count=1 ./... and go vet ./.... Review all changes before integration; deliver tested head and download link.
