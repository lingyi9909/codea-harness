# 1.6.6 implementation plan

1. Add primary-session and actual-user selection attestation regression cases.
2. Extend the existing submission tool and Runtime authority for primary submissions; keep legacy validation.
3. Bind multi-chain scope to current options and verified user selection at select, units and report boundaries.
4. Normalize request BOM ingress with strict parsing and unchanged artifact hashes.
5. Update active Agent instructions and versioned Host upgrade ownership; retain old release scripts.
6. Run focused and full Linux regression, independent review, then exact-head Windows package and upgrade regression.
7. Integrate tested head and provide run/job/package evidence.
